#include <sys/select.h>
#include <sys/socket.h>

#include "cabecalho.h"

int main(int argc, char const *argv[]) {   
    uint16_t nextId = 0;
    uint16_t sequ = 0;

    fd_set rfds;

    exit(0);
}