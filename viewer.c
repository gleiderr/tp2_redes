#include "cabecalho.h"

int openSocket(char const* addr);

int main(int argc, char const *argv[]) {   
    uint16_t myId;
    uint16_t sequ = 0;

    //Enviar mensagem de OI para servidor:
    
    exit(0);
}